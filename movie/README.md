# Спроектировать схему данных для системы управления кинотеатром

### Основная задача
    * Кинотеатр имеет несколько залов, в каждом зале идет несколько разных сеансов, клиенты могут купить билеты на сеансы
    * Спроектировать базу данных для управления кинотеатром
    * Задокументировать с помощью логической модели
    * Написать DDL скрипты
    * Написать SQL для нахождения самого прибыльного фильма

------------

### Результат

------------
1. Подготовлен словарь данных с описанием БД

    - DICT.md

2. Подготовлена схема модели

    - ER_DIAGRAM.md
    - cinema_schema.png

3. Написаны DDL скрипты

    - init-db/01-schema.sql

4. Написаны скрипты для поиска самого прибыльного фильма

    - total_revenue.sql

## Дополнительно. Можно посмотреть БД в pgAdmin

**Запустить Docker, выполнив команду** (не забыть переименовать env файл)

```
docker compose up --build -d
```

- сбросить volume

```
docker compose down -v
docker compose up -d
```

**Быстрая проверка**

```
docker exec -it postgres psql -U postgres -d cinema

SELECT * FROM cinema.movies;
```

**Подключение через pgAdmin**

http://localhost:5050

Логин: admin@admin.com
Пароль: 112233

Приложение pgAdmin запускается с задержкой!!!