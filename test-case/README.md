### 1. Модульные тесты

    1.1. Кейсы проверки валидации card_number

| Правила валидации | Код ошибки |
|-------------------|------------|
| card_number отсутствует | 400 |
| card_number пустой | 400 |
| card_number содержит менее 16 цифр | 400 |
| card_number содержит более 16 цифр | 400 |
| card_number содержит буквы | 400 |
| card_number содержит спецсимволы | 400 |
| card_number корректный | 200 |

    1.2. Кейсы проверки валидации card_holder

| Правила валидации | Код ошибки |
|-------------------|------------|
| card_holder отсутствует | 400 |
| card_holder пустой | 400 |
| card_holder содержит цифры | 400 |
| card_holder содержит кириллицу | 400 |
| card_holder содержит спецсимволы кроме дефиса | 400 |
| card_holder содержит два пробела подряд | 400 |
| card_holder более одного разделителя между именем и фамилией | 400 |
| card_holder только имя | 400 |
| card_holder содержит имя и фамилию латиницей | 200 |

    1.3. Кейсы проверки валидации card_expiration

| Правила валидации | Код ошибки |
|-------------------|------------|
| card_expiration отсутствует | 400 |
| card_expiration пустой | 400 |
| card_expiration содержит буквы | 400 |
| card_expiration неверный формат | 400 |
| card_expiration формат месяца неверный (00 или больше 12) | 400 |
| card_expiration карта просрочена | 400 |
| card_expiration карта действительна | 200 |

    1.4. Кейсы проверки валидации cvv

| Правила валидации | Код ошибки |
|-------------------|------------|
| cvv отсутствует | 400 |
| cvv пустой | 400 |
| cvv менее 3 цифр | 400 |
| cvv более 3 цифр | 400 |
| cvv содержит буквы | 400 |
| cvv корректный CVV | 200 |

    1.5. Кейсы проверки валидации order_number

| Правила валидации | Код ошибки |
|-------------------|------------|
| order_number отсутствует | 400 |
| order_number пустой | 400 |
| order_number длина > 16 символов | 400 |
| order_number длина < 16 символов | 400 |
| order_number длина = 16 символов | 200 |

    1.6. Кейсы проверки валидации sum

| Правила валидации | Код ошибки |
|-------------------|------------|
| sum отсутствует | 400 |
| sum пустая строка | 400 |
| sum отрицательная сумма | 400 |
| sum ноль | 400 |
| sum формат суммы через точку | 400 |
| sum не число (содержит не числовые символы, кроме 1 запятой) | 400 |
| sum корректный формат (числовая строка через запятую) | 200 |

    1.7. Кейсы проверки взамиодействия с сервисом А (сервис А замокан)

| Ответ от сервиса А | Реакция бэкенда  |
|-------------------|------------|
| вернул 403 | возвращает 403 |
| вернул 200 | вызывает репозиторий |
| недоступен | возвращает 500 |

    1.8. Кейсы проверки взамиодействия с репозиторием (репозиторий замокан)

| Ответ от репозитория | Реакция бэкенда  |
|-------------------|------------|
| setOrderIsPaid() вернул true | возвращает 200 |
| Репозиторий выбросил исключение | возвращает 500 |
| При ошибке сервиса A репозиторий не вызывается | возвращает 403 |


### 2. Интеграционные тесты

    2.1. Проверка связки «фронт-бэк»:
        2.1.1 Счастливый путь: При отправке с фронта формы с валидными данными, фронт получает от бэка HTTP 200 и перенаправляет пользователя на страницу «Оплата успешно завершена».
        2.1.2. Ошибка валидации: 
            - пользователь вводит 2 цифры cvv, фронт получает от бэка HTTP 400 и подсвечивает поле «CVV» красной рамкой, выводя под ним сообщение «Неверный формат кода».
            - пользователь вводит card_number = 123, фронт получает от бэка HTTP 400 и подсвечивает поле «Номер карты» красной рамкой, выводя под ним сообщение «Неверный номер карты».
            - пользователь вводит card_expiration = 15/25, фронт получает от бэка HTTP 400 и подсвечивает поле «Срок» красной рамкой, выводя под ним сообщение «Неверная дата действия карты».  
        2.1.3. Отказ банка: Если бэк принудительно отвечает ошибкой списания, фронт выводит модальное окно: «Не удалось списать данные с карты, проверьте реквизиты».

    2.2. Проверяем связку «бэк-репозиторий»:
        2.2.1. Счастливый путь: При получении валидных данных бэкендом, он успешно конвертирует строку суммы с запятой во float и вызывает реальный метод setOrderIsPaid. 
        2.2.2. Несовпадение данных: Бэкенд вызывает метод setOrderIsPaid для номера заказа, сумма которого в БД отличается от переданной в запросе. Проверяем, что репозиторий выбрасывает ожидаемое исключение.

    2.3. Проверяем связку «бэк-сервис А»:
        2.3.1. Счастливый путь: Бэк отправляет запрос с валидными реквизитами "успешной" тестовой карты в API Сервиса А. Проверяем, что бэкенд корректно парсит ответ и интерпретирует его как HTTP 200.
        2.3.2. Недостаточно средств: Бэк отправляет запрос с реквизитами невалидной тестовой карты в API. Проверяем, что бэкенд корректно принимает HTTP 403 от сервиса и не падает с фатальной ошибкой.
        2.3.3. Сервис A недоступен.  Проверяем, что бэкенд корректно возвращает 500 ошибку. 

### 3. Системные тесты

    3.1. Счастливый путь: Пользователь открывает страницу оплаты, вводит корректные данные тестовой карты, нажимает «Оплатить». Фронт показывает спиннер загрузки. Запрос проходит через бэк, Сервиса А списывает виртуальные средства, БД обновляет статус. Пользователь видит экран «Заказ №... успешно оплачен».

    3.2. Отказ в проведении операции: Пользователь вводит реквизиты тестовой карты с истекшим сроком действия или заблокированной карты. Система отрабатывает полный цикл (запрос уходит в Сервис А, возвращается 403, БД не обновляется). После загрузки фронт отображает уведомление: «Не удалось списать данные с карты, проверьте реквизиты», поля ввода остаются заполненными (кроме CVV) для повторной попытки.

    3.3. Ошибка сохранения в БД. Пользователь проходит оплату успешно. Сервис A возвращает 200. Repository выбрасывает исключение. Проверяем, что пользователю показано сообщение о технической ошибке, информация об ошибке залогирована.

    3.4. Полностью невалидные данные. Пользователь отправляет форму с пустыми полями. Проверяем, что все ошибочные поля подсвечены, отображаются сообщения валидации, запрос на сервис A не отправляется.

### 4. Решение проблемы удешевления тестирования 

    4.1. Способы решения:
        - у любого адекватного платежного шлюза есть тестовый контур и набор тестовых карт (к примеру, в платежном шлюзе Сбербанка). Каждая карта жестко привязана к определенному ответу системы. 
        - если нет тестового API, для системных тестов поднимается внутренний микросервис-заглушка, который эмулирует ответы Сервиса А на основе заранее заданных правил.
        - на проде можно создать заказ стоимость за 1 рубль и провести покупку со своей карты. 
        
  
