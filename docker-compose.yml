services:
  postgres:
    image: postgres:latest
    environment:
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: secret
      POSTGRES_DB: mapper
    volumes:
      - postgres:/var/lib/postgresql
    ports:
      - "5432:5432"

volumes:
  postgres:
