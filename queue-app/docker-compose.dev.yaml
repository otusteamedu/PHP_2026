services:
  nginx:
    build:      
      target: dev
      dockerfile: ./docker/nginx/Dockerfile
    volumes:
      - ./:${APP_PATH}

  php:
    build:     
      target: dev
      dockerfile: ./docker/php/Dockerfile  
    volumes:
      - ./:${APP_PATH}

  queue_worker:
    build:    
      target: dev
      dockerfile: ./docker/php/Dockerfile         
    volumes:
      - ./:${APP_PATH}